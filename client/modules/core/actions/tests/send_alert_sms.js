const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../send_alert_sms';

describe('core.actions.send_alert_sms', () => {
  it('should do something');
});
