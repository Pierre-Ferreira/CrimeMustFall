const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../connect_contact_request';

describe('core.actions.connect_contact_request', () => {
  it('should do something');
});
