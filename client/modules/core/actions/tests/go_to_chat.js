const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../go_to_chat';

describe('core.actions.go_to_chat', () => {
  it('should do something');
});
