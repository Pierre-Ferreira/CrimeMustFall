const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../emergency_contacts_search';

describe('core.actions.emergency_contacts_search', () => {
  it('should do something');
});
