const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../settings_user_info';

describe('core.actions.settings_user_info', () => {
  it('should do something');
});
