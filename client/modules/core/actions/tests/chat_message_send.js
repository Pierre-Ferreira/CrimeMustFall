const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../chat_message_send';

describe('core.actions.chat_message_send', () => {
  it('should do something');
});
