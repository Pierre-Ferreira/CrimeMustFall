const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../contact_connect_request_confirm';

describe('core.actions.contact_connect_request_confirm', () => {
  it('should do something');
});
