const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import MyChatsAlertedButton from '../my_chats_alerted_button';

describe('core.components.my_chats_alerted_button', () => {
  it('should do something');
});
