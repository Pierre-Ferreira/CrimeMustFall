const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ContactsRequestedByMeList from '../contacts_requested_by_me_list';

describe('core.components.contacts_requested_by_me_list', () => {
  it('should do something');
});
