const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ContactsInfoCardSmall from '../contacts_info_card_small';

describe('core.components.contacts_info_card_small', () => {
  it('should do something');
});
