const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import EmergencyContactsSearch from '../emergency_contacts_search';

describe('core.components.emergency_contacts_search', () => {
  it('should do something');
});
