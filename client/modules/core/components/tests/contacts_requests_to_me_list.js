const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ContactsRequestsToMeList from '../contacts_requests_to_me_list';

describe('core.components.contacts_requests_to_me_list', () => {
  it('should do something');
});
