const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ChatMessagesDisplay from '../chat_messages_display';

describe('core.components.chat_messages_display', () => {
  it('should do something');
});
